generator client {
  provider = "prisma-client-js"
}

generator kysely {
  provider     = "prisma-kysely"
  output       = "../types"
  fileName     = "Types.ts"
  enumFileName = "Enums.ts"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
